emmetcheatsheet - Shortcuts ---- https://docs.emmet.io/cheat-sheet/

button.btn.btm-primary{primary button}
#div1 ---- "#" represents id
.div2 ---- "." represents class
button.btn.btm-primary{primary button}

ol>li*5{Country-$}

select>option*20{Country-$}


table>tbody>tr*50>td*5{Data-$}

Use CSS in HTML
---------------------
1. Inline CSS    (style attribute)
    <div style='color':red> This is CSS div </div>
    limitation: Restriceted to only one element tag
2. Internal CSS  (Style tag/attribute)  (Restricted to one HTML page)
3. external CSS - Using link 
    Write in separate CSS file and import it in HTML file using link tag
        <link rel = stylesheet" href="demo_1.css">


How to use Bootstrap
----------------------------
Add CDN tag to Head in HTML file
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" 
integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">



How to execute JS code
------------------------------------
1. Browser (Frontend Development)
    a. Create a JS file 
    b. include that JS in HTML file suing script tag
    c. open the HTML file in browser (alt+b)
2. Node]S (BackEnd Development)
    a. create a javascript file with some javascript content (abc.js)
    b. Run that javascript file with the below command node ‹ filename>.js (make sure nodejs is installed)
Note: Every javascript code that runs on browser might not run in nodejs window.alert( Addition of ${a) and ${b} is $(a+b]*); // this statement only works in browser, not in nodejs




Most commonly used JS functions
------------------------------------
console.log()   --- cosole is one object and log is a function
document.write() -- document is object and write is function
window.alert()   -- window is object and alert is function

log("This is a cosole HEllo in HTML file");   //Not allowed
write("This is a document write Helloooooodw HTML file");   //Not allowed
alert("This is a alert HelloWA in HTML file");  //Allowed

Note: While accessing window object methods/functions, using window.function() is not mandatory


Most commonly used JS objects
-----------------------------------------
window      - alert(), cofirm(), print(), close(), prompt()
console     - log(), error(), warn(), table(), timeEnd(), asser(), time()
document    - write(), read(), writeln()
array       - push(), pop(), filter(), map()
math        - abs(), sqrt(), cbrt(), max(), min(), sum()
object      - assign(), create(), freeze()

alert = message+OK button

confirm() - Collect User confirmation, Yes/Node
            e.g:- deleting or Save some content

Prompt()     Input box from user for collection, OK and Cancel